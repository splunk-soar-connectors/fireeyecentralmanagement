* Enable TLS certificate verification by default
* Escape alert and quarantined-email identifiers before embedding them in widget JavaScript
* Encode quarantined-email queue IDs in request paths and use unique vault staging filenames
* Mark get quarantined email non-read-only because it writes to the SOAR vault